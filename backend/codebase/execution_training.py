# execution_training.py
# Training examples for Execution Awareness

EXECUTION_EXAMPLES = [
    {
        "vague": "Fetch stock data",
        "execution": "Use the yfinance Python library to fetch historical stock prices for a given ticker symbol. The function should accept a symbol and date range, handle API errors, and return data as a pandas DataFrame."
    },
    # Add more examples as needed
]
